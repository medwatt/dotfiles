require("fzf-lua").setup({
    files = {
        formatter = "path.filename_first"
    },
    grep = {
        formatter = "path.filename_first"
    },
    buffers = {
        formatter = "path.filename_first"
    }
})

